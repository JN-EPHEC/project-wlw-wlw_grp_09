rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Autoriser la lecture et l'ecriture pour tous.
    // ATTENTION: ces regles ne sont pas securisees pour la production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
