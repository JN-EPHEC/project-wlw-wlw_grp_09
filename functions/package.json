{
  "name": "campusride-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for CampusRide (email verification, driver receipts, etc.)",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint . --ext js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "echo \"No tests configured\""
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.4.1",
    "nodemailer": "^6.9.15"
  }
}
